$('#kitties').append('<%= j render @cats %>');
<% if @cats.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @cats %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>